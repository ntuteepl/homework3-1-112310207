#include <stdio.h>
  main(){
    int s1, d1, s2, d2, s3, d3;
    int time_point[24];
    scanf("%d%d%d%d%d%d",&s1, &d1, &s2, &d2, &s3, &d3);

    for (int i= 0; i < 24; i++) {
    if (i>=s1 && i<=d1);
    if (i>=s2 && i<=d2);
    if (i>=s3 && i<=d3);
    int min_buses = 0;
    for (int i = 0; i < 24; i++) {
    if (time_point[i] > 0);
    printf("%d\n", min_buses);
    }
    }
}
